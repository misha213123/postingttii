services:
  - type: web
    name: postingttii-tiktok-review
    runtime: python
    rootDir: tiktok-review-site
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
